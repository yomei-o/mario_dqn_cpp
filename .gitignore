# build artifacts
build/
wasmdist/
wasmtest/
*.obj
*.exe
*.lib
*.exp
*.pdb

# trained weights / checkpoints (keep the released Mario checkpoint)
*.bin
*.latest_*
!mario_best.bin
# reusable warm-start library (curated nets + demos)
!warmstarts/*.bin

# NES ROMs are copyrighted -- keep them out of code commits (supply your own)
*.nes

# training logs
*.log
*.err

# python / editor
__pycache__/
*.pyc
.vs/
.claude/
*.user
*.out
!demo_clear_1-1.bin
